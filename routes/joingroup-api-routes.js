var db = require("../models");

module.exports = function(app){

    //a get for route /api/joingroup that displays something

    //a get for route /api/joingroup/:id that displays something of one group

    //a post for route /api/joingroup that adds to a table

    // a put for route /api/joingroup that modifies an existing group

    //a delete for route /api/joingroup that removes user from group
}